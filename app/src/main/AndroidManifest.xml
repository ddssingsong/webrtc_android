<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dds.webrtc">

    <application
        android:name="com.dds.BaseApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name="com.dds.voip.FloatingVoipService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name="com.dds.LauncherActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- nodejs -->
        <activity
            android:name="com.dds.nodejs.NodejsActivity"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- java -->
        <activity
            android:name="com.dds.java.JavaActivity"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- 单聊界面 -->
        <activity android:name="com.dds.voip.SingleCallActivity">
            <intent-filter>
                <action android:name="${applicationId}.kit.voip.single" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver android:name="com.dds.voip.VoipReceiver">
            <intent-filter>
                <action android:name="${applicationId}.voip.Receiver" />
            </intent-filter>
        </receiver>
    </application>

</manifest>